<?php

namespace Multiple\Frontend\Controllers;

use Phalcon\Mvc\Controller;
use Multiple\Frontend\Models\NbaGames;
use Multiple\Frontend\Models\NbaImages;
use Multiple\Frontend\Models\NbaTags;

class IndexController extends Controller
{
    public function indexAction()
    {
        //$game = NbaGames::findFirst();
        //var_dump($game->toArray(), $game->getImages());exit;
        //$image  = NbaImages::findFirst();
        //var_dump($image->toArray(), $image->getGame());exit;
        //$images = NbaImages::findFirst();
        //var_dump($images->toArray(), $images->getTags());exit;
        //$tag = NbaTags::findFirst();
        //var_dump($tag->toArray(), $tag->images);exit;
    }
}
